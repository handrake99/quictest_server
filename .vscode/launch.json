{
    "version": "0.2.0",
    "configurations": [
        {
            "workspaceFolder": "/Users/handrake/work/quickflow_test",
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/quicflow_echo_server",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            //"MIMode": "lldb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            // 디버깅 전 자동으로 빌드하고 싶다면 아래 "preLaunchTask" 추가 (선택사항)
            // "preLaunchTask": "CMake: build", 
            // "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}